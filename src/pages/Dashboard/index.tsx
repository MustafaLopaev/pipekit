import './index.css';

// Interface for props
interface DashboardInterface {}

/* Dashboard page is used to display the general data. */
const Dashboard = (props: DashboardInterface) => {
  return <div id="dashboardPage" className="container-fluid"></div>;
};

export default Dashboard;
